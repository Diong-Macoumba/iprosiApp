import { CourseResponseDto } from './course-response-dto';

describe('CourseResponseDto', () => {
  it('should create an instance', () => {
    expect(new CourseResponseDto()).toBeTruthy();
  });
});

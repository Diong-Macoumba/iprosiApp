import { StudentResponseDto } from './student-response-dto';

describe('StudentResponseDto', () => {
  it('should create an instance', () => {
    expect(new StudentResponseDto()).toBeTruthy();
  });
});

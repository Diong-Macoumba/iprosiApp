import { TeacherResponseDto } from './teacher-response-dto';

describe('TeacherResponseDto', () => {
  it('should create an instance', () => {
    expect(new TeacherResponseDto()).toBeTruthy();
  });
});
